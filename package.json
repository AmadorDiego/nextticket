{
  "name": "nextticket",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "prepare": "husky install",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.12.3",
    "commitlint": "^17.0.0",
    "@commitlint/config-conventional": "^17.0.0",
    "husky": "^9.0.0"
  }
}
